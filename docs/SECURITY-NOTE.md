# セキュリティに関する注意事項

このフォルダ（`docs/`）は GitHub Pages として**全ファイルが公開**されます。

- APIキー・トークン・パスワードなど、機密情報を含むファイルをこのフォルダ配下に置かないでください。
- 設定値（APIキー・シークレット等）は GAS（Google Apps Script）の**スクリプトプロパティ**に保管し、クライアント（このフォルダ内のJS/HTML）には渡さない構成としてください。
